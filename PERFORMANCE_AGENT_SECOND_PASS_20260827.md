# 聊天气泡动画第二轮：Agent 实施建议（真机复盘版）

## 1. 任务目标

本轮只解决一个已经由真机 trace 证实的问题：

> 用户仅滑动聊天列表、没有按压消息气泡时，气泡组件更新仍大量进入 `duration: 110ms` 动画任务，并与列表测量、组件重建叠加，制造长尾掉帧。

先完成“零隐式气泡动画”基线，再决定是否恢复平滑按压效果。不要同时调整 `cachedCount`、复用回调、时间格式化或列表加载策略，否则无法判断收益来自哪里。

验收结果应满足：

- 纯滑动期间，`MessageBubble` / `LightweightPlainTextBubble` 的普通数据更新不再进入 110ms 动画上下文。
- 按压反馈至少仍可用；第一阶段允许改为立即变化，不要求保留过渡动画。
- 不破坏左右滑动回复、长按菜单、链接点击、音频/文件操作、消息选择和气泡复用。
- 不改动消息首次展示的逐条淡入方案。

## 2. 背景与最新证据

对比报告：

- 优化前：`Frame_PLA-AL10_com.miramira8295.petrelgram_Main Process_20260827T093046.insight`
- 优化后：`Frame_PLA-AL10_com.miramira8295.petrelgram_Main Process_20260827T104928.insight`

可比滑动场景的主要结果：

| 指标 | 优化前 | 优化后 | 判断 |
| --- | ---: | ---: | --- |
| 全程超预算帧占比 | 25.1% | 12.1% | 明显改善 |
| 全程平均帧耗时 | 9.997ms | 6.799ms | 明显改善 |
| 滑动平均帧耗时 | 10.157ms | 7.157ms | 改善 29.5% |
| 滑动超预算帧占比 | 28.7% | 13.3% | 已达到上一轮 `<15%` 目标 |
| P99 | 61.081ms | 32.990ms | 明显改善 |
| 最大帧 | 210.255ms | 463.412ms | 长尾反而恶化 |

时间缓存已经有效：时间格式化相关 CPU 从约 `826.3ms` 降至 `199.4ms`，不应在本轮回退。

气泡动画仍未被真正隔离：

- 滑动期间，`duration: 110ms` 动画片段只从 1329 次降至 1261 次，次数仅减少约 5.1%。
- 新报告最重的 463ms 帧同时出现 24 次组件构建、25 次渲染更新、24 个 110ms 动画片段和 35 次列表项测量。
- 该帧中可以看到 `duration: 110 -> FlushLayoutTask -> Measure[List]` 链路。
- 新报告中，同时发生组件构建和渲染更新的帧，平均约 24.6ms，超预算率约 78.8%。

结论：把 `.animation(...)` 在属性链中换位置，只降低了一部分开销，没有建立可靠的动画边界。ArkUI 在组件更新时仍会创建动画上下文，并把同一刷新周期内的列表脏节点带入动画布局。

## 3. 当前相关代码

重点文件：

- `entry/src/main/ets/pages/chat/MessageBubble.ets`
- `entry/src/main/ets/pages/chat/LightweightPlainTextBubble.ets`
- `entry/src/main/ets/util/scrollEffects.ets`
- `entry/src/test/ScrollEffects.test.ets`

当前问题形式大致为：

```ts
.stateStyles({
  normal: this.normalStyle,
  pressed: this.pressedStyle
})
.animation({ duration: this.persistentEffectDuration(110) })
```

即使几何属性写在 `.animation` 之后，真机 trace 仍证明普通组件更新会落进这段动画。

注意：`persistentAnimationDurationFor` 还可能用于定位、脉冲等其他长期动画。本轮不要全局删除该工具函数，只处理气泡根节点的 110ms 按压动画调用。

## 4. 实施顺序

### 阶段 A：建立零隐式动画基线（P0，必须先做）

#### A1. 完整气泡

在 `MessageBubble.ets` 中：

1. 找到承载气泡 `stateStyles` 的节点。
2. 仅移除紧跟其后的 `duration: 110ms` 永久 `.animation(...)` 修饰器。
3. 保留现有 `normal` / `pressed` 样式，使按压时的透明度和缩放立即切换。
4. 不改左右滑动回复手势、长按菜单、点击事件和外层用于清理 `gestureClaimed` 的触摸监听。
5. 不删除该文件中其他明确用于定位、脉冲等效果的动画。

#### A2. 轻量纯文本气泡

在 `LightweightPlainTextBubble.ets` 中做相同处理：

1. 移除气泡根节点 110ms 的永久 `.animation(...)`。
2. 保留 `stateStyles`，第一阶段按压反馈改为瞬时切换。
3. 如果该组件的 `persistentEffectDuration` 方法因此已经完全无引用，可以删除组件内的无用封装；不要因此删除公共工具。

#### A3. 修正误导性注释和测试

检查 `scrollEffects.ets` 与 `ScrollEffects.test.ets`：

- 不要再用注释或测试声称“只要调整属性链顺序，就能保证气泡动画不覆盖布局”。真机报告已经否定这个假设。
- 可以继续保留“长期视觉效果不因列表滚动直接关闭”的通用工具测试，因为其他效果仍可能使用它。
- 单元测试不要假装能证明 ArkUI 运行时的动画隔离；这个结论必须由真机 trace 验证。

#### A4. 本阶段禁止项

本次提交中不要：

- 修改 `.cachedCount(4, true)`。
- 修改 `ReusableMessageBubble` 的 `setTimeout(0)`。
- 修改消息初始 90ms 淡入。
- 修改顶部、输入框或页面转场。
- 引入新的延迟任务、批量加载策略或列表 key。
- 顺手清理其他代码。

这样可以把真机收益准确归因于“移除永久气泡动画上下文”。

### 阶段 B：按真实触摸事件恢复平滑反馈（P1，A/B 通过后再做）

阶段 A 如果滑动改善明显，但瞬时按压手感不能接受，再单独提交阶段 B。不要重新加回永久 `.animation(...)`。

推荐实现：

1. 在两个气泡组件内维护局部按压显示状态，例如透明度与缩放值。
2. 气泡表面收到 `TouchType.Down` 时，通过一次显式 `animateTo` 把状态切到 pressed。
3. 收到 `TouchType.Up` 或 `TouchType.Cancel` 时，通过一次显式 `animateTo` 恢复 normal。
4. `.opacity(...)` 和 `.scale(...)` 只读取状态值，本身不要挂永久 `.animation(...)`。
5. 在复用、回收和消息身份切换时，直接把状态重置为 normal，不播放恢复动画。

示意结构，仅表达边界，不要求照抄命名：

```ts
@State private bubblePressOpacity: number = 1.0
@State private bubblePressScale: number = 1.0

private setBubblePressed(pressed: boolean): void {
  const targetOpacity = pressed ? 0.84 : 1.0
  const targetScale = pressed ? 0.992 : 1.0
  animateTo({ duration: 110, curve: Curve.EaseOut }, () => {
    this.bubblePressOpacity = targetOpacity
    this.bubblePressScale = targetScale
  })
}
```

实现时必须处理：

- 省电模式或消息动画关闭时，直接赋值，不创建动画。
- 现有不需要整气泡按压反馈的类型继续排除，例如内联链接、语音、音频、文件等，以现有 `pressedStyle` 条件为准。
- 把触摸监听加在真正的气泡表面，不要笼统放到整行，否则点击头像、链接或行内控件也会让整颗气泡缩放。
- 保留已有外层 `onTouch` 的手势清理职责，不要覆盖或删除。
- 垂直拖动开始后应收到 `Cancel` 并恢复 normal；左右滑动回复仍须正常竞争手势。
- `onTouch` 只观察事件，不应吞掉子组件点击。
- 回收/复用时立即重置，避免上一条消息残留 pressed 状态。

此方案的关键不是视觉数值，而是动画只由真实 `Down / Up / Cancel` 事件启动。消息状态、下载进度、时间、翻译、复用等普通数据更新不能启动按压动画。

## 5. 验证方法

### 5.1 静态检查

提交前确认：

- 两个气泡根节点上都不存在永久的 110ms `.animation(...)`。
- 其他动画没有被误删。
- 没有重复的 `onTouch` 覆盖已有监听。
- 回收和复用路径会清理新增的按压状态。
- 代码格式、类型检查和现有测试通过。

建议执行仓库已有的构建与测试流程，并运行 `git diff --check`。不要为了本任务修改不相关的失败用例。

### 5.2 功能回归

至少手工验证：

- 普通文本气泡按下、抬起、取消。
- 富文本、图片、视频、语音、音频、文件、链接消息。
- 长按菜单。
- 左滑回复。
- 点击链接和行内按钮。
- 快速上下滑动时手指按下后转为滚动。
- 气泡滚出屏幕后复用，不残留缩放或半透明。
- 省电模式/关闭消息动画时不创建动画。

### 5.3 真机 A/B 录制脚本

阶段 A 的性能录制不要混入点击动作：

1. 打开同一个长聊天，等待 1 秒。
2. 连续进行约 30 次方向和力度接近的列表 fling。
3. 期间不要点击、长按任何气泡，不要打开菜单。
4. 最后等待 1 秒并结束录制。

按压手感另录一份短 trace：连续点击 5 次普通文本气泡，确认每次最多只有一次按下和一次恢复动画。不要拿这份 trace 评价纯滑动性能。

### 5.4 通过标准

阶段 A 的硬标准：

- 纯滑动 trace 中，不能再出现由 `MessageBubble` 或 `LightweightPlainTextBubble` 普通更新触发的 `duration: 110 -> FlushLayoutTask -> Measure[List]` 链路。
- 纯滑动中的 110ms 事件若仍存在，必须定位其父调用；不能只看全局数量，因为菜单、反应或其他组件也可能使用 110ms。
- 与最新报告同场景相比，滑动超预算帧占比应从 13.3% 继续下降，目标不高于 10%。
- 由“气泡更新 + 110ms 动画 + List 测量”造成的 50ms 以上帧应为 0。
- 左滑、长按、点击、复用均无功能回归。

阶段 B 的附加标准：

- 无触摸的纯滑动结果不能明显回退。
- 只有真实触摸事件能启动气泡按压动画。
- 一次点击最多产生按下、恢复两段短动画，不随消息刷新重复触发。

## 6. 后续工作顺序

本轮通过后再按以下顺序分别做实验，每项单独提交、单独录制：

1. `ReusableMessageBubble` 复用后的 `setTimeout(0)`：先加计数和耗时证据，确认高频后再考虑合并或去除。最新报告中其 CPU 约 351ms，但它负责复用后重新挂载展示状态，不能直接删除。
2. 日期标签的 `monthDay` 缓存：消息时钟已经优化，剩余时间格式化热点主要在日期标签，约 142ms。
3. `cachedCount` A/B：最后再测试 `6 / 4 / 2`，并把第二个参数 `show=true/false` 一起纳入矩阵。`show=true` 会让缓存项参与渲染，不能只改单个数字。
4. `CachedImage` 延迟任务：先排除本次消息内容差异，再决定是否优化。

关闭聊天页面的转场不在最新 trace 内：报告在页面切换开始前已经结束。不要用这份报告宣称关闭卡顿已经解决，关闭场景需要单独录制。

## 7. Agent 交付要求

交付时请明确列出：

- 修改了哪些文件、每个文件解决什么问题。
- 当前完成的是阶段 A 还是阶段 B。
- 构建、测试和手工回归结果。
- 是否安装真机；未得到明确授权时只构建，不安装、不覆盖设备应用。
- 新 trace 中，气泡 110ms 动画次数、滑动超预算率、`>50ms` 帧数、最大帧及最重调用链。
- 未解决问题和下一步建议。

不要自动提交、推送或合并，也不要重置工作区中的其他修改，除非用户明确要求。
