// bindSheet 自带装饰件的尺寸，供半模态面板的自绘标题行让位用。

// 系统关闭按钮（右上角那个圆形 ×）要占掉的右侧宽度，vp。
//
// **为什么需要它。** bindSheet 默认 showClose: true，关闭按钮是框架**盖在**面板
// 内容之上的一层，不参与内容的布局——面板里的 Row 该多宽还是多宽。于是任何把
// 操作按钮右对齐的自绘标题行都会被它压住：用户报的是通话设置→例外名单里
// 「完成」几乎完全被 × 盖掉，只露出半个「完」字（issue #124）。
//
// 40 是关闭按钮本身的直径，8 是它到面板右缘的外边距，合计 48。让位的做法是给
// 标题行加右内边距，而不是关掉 showClose：滑动关闭对半模态面板不是人人都知道，
// 留一个明确的关闭入口比省这 48vp 值。
export const SHEET_CLOSE_RESERVE_VP: number = 48;
